# Variáveis de ambiente de exemplo (nunca commitar .env real)
# Use este arquivo como referência para configurar segredos localmente

# Credenciais de login UOL/BOL (preferencialmente use config.properties)
UOL_EMAIL=seu_email@bol.com.br
UOL_PASSWORD=sua_senha

# AWS (apenas para deploy via Terraform)
AWS_REGION=us-east-1
